type List = 
 opt record {
       record {
         principal;
         principal;
       };
       List;
     };
type AssocList = 
 opt record {
       record {
         principal;
         principal;
       };
       List;
     };
service : {
  get_balance: (principal) -> ();
  get_registered_ids: (principal) -> (opt principal);
  register_id: (principal, opt principal) -> (AssocList, opt principal);
  view_registered_ids: () -> (AssocList);
  whoami: () -> (principal);
}
